# AGENTS.md

## 工作方式

- 默认使用自然、准确的中文交流；复杂内容优先用完整段落，确有必要时再列表。
- 开始修改前先阅读 `README.md`、`docs/prototype-scope.md` 和 `docs/assumptions-and-decisions.md`。
- 未确认的产品或技术选择必须标为假设或待决策，不得写成既定事实。
- Phase 1 是知识模型研究，不开始三维可视化或前端原型。
- 六类对象、四类关系和“基础知识在中心”都只是候选假设，不得固化为架构。
- 本体/身份、层级、先修、语义相似、图连接性、径向位置和视觉距离必须分别表述。
- 工作以两周、30–100 个实体的建模实验为边界；新增范围前先说明取舍。
- 数据样例必须附来源、许可或人工构造说明；不要提交密钥、个人数据或大体积原始语料。
- 修改后运行当前可用的检查，并说明未能验证的部分。

## 目录约定

- 产品与架构说明放在 `docs/`，确认的重要决策放在 `docs/decisions/`。
- 研究脚本放在 `src/`，实验数据放在 `data/sample/`，校验与可重复性测试放在 `tests/`。
- 临时分析放在 `work/`；用户交付物放在 `outputs/`，两者都不提交。
